<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.rescuenow_dev">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".patient.home.InjuriesActivity"></activity>
        <activity android:name=".patient.home.HeartRelatedActivity" />
        <activity android:name=".patient.home.FeverRelatedActivity" />
        <activity android:name=".patient.home.BasicFirstAidActivity" />
        <activity android:name=".diseases.DieseasesDetails" />
        <activity android:name=".diseases.SymptomsListActivity" />
        <activity android:name=".patient.PatientDashboardActivity" />
        <activity android:name=".doctor.DoctorDashboardActivity" />
        <activity android:name=".common.LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".common.SignupActivity" />
        <activity android:name=".common.SplashScreenActivity" />
        <activity android:name=".common.MainActivity" />

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>